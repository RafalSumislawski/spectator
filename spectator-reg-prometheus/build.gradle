dependencies {
  api project(':spectator-api')
  implementation 'io.prometheus:simpleclient:0.9.0'
}

jar {
  manifest {
    attributes(
      "Automatic-Module-Name": "com.netflix.spectator.prometheus"
    )
  }
}
